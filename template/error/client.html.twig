{% extends "single_layout.html.twig" %}

{% block title %}{{ title }}{% endblock %}

{% block container %}
<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="clearfix">
            <h1 class="float-left display-3 mr-4">{{ status }}</h1>
            <h4 class="pt-3">{{ message }}</h4>
            <p class="text-muted">ホームに戻る場合は<a href="{{ path_for('homepage') }}">こちら</a>。</p>
        </div>
    </div>
</div>
{% endblock %}
